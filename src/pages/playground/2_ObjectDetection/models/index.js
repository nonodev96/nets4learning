/**
 * @typedef {Object} TYPE_MODEL_OBJECT_DETECTION
 * @property {string} KEY
 * @property {string} TITLE
 * @property {string} URL
 * @property {string} URL_MODEL
 * @property {JSX.Element} DESCRIPTION
 * @property {JSX.Element} HTML_EXAMPLE
 */
import { MODEL_FACE_DETECTOR } from "./MODEL_FACE_DETECTOR";
import { MODEL_FACE_MESH } from "./MODEL_FACE_MESH";
import { MODEL_MOVE_NET_POSE_NET } from "./MODEL_MOVE_NET_POSE_NET";
import { MODEL_COCO_SSD } from "./MODEL_COCO_SSD";

export {
  MODEL_FACE_DETECTOR,
  MODEL_FACE_MESH,
  MODEL_MOVE_NET_POSE_NET,
  MODEL_COCO_SSD
}